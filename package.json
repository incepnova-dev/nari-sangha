{
  "name": "nari-sangha-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:api-client": "cd packages/api-client && npm run build",
    "watch:api-client": "cd packages/api-client && npm run watch"
  }
}

