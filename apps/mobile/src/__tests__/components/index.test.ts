/**
 * Test suite index - runs all component tests
 * This file can be used to run all component tests together
 */

describe('Component Test Suite', () => {
  it('should have all test files configured', () => {
    // This test ensures the test suite is properly set up
    expect(true).toBe(true);
  });
});

